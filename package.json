{
  "name": "freev-valeur",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:browser": "node scripts/browser-smoke.mjs",
    "validate": "node scripts/validate.mjs && node --test"
  }
}
